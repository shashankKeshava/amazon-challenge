const initialState = {
    resturant: [],
    isLoading: true,
    type: "FETCH_TYPE"
};


const Amazon=(state=initialState,action)=>{
    switch(action.type){
        default: return state;
    }

}

export default Amazon;